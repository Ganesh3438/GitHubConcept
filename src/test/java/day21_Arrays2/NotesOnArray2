Question 1: Given an array of size n and a value is k. Write a function that returns true 
if K is present in array otherwise return false

Answer:  public boolean search(int[] arr, ink){
          
          int len = arr.length
          
          for(int i=0; i<len; i++){
          
          if(arr[i]===k){
          return true;
          }
          return false;
          }

         }
         
Question 2: Given an array of size n and a value k. Write a function that return frequency of k in arr

Answer: public static int count_element(int arr[], int k){
        
        int len = arr.length;
        int count = 0;
        for(int i=0; i<len; i++){
        if(arr[i]==k){
        count = count + 1;
        }
        }
        return count;
}         

Question 3: Given an array of size n. Write a function that returns true if consecutive duplicates
are present in array otherwise return false

Answer: public static boolean check_consecutivenumbers(int arr[], int k){
     
        for(int i=0; i<arr.length; i++){
        if(arr[i] == arr[i+1]){
        return true;
        }
        }
        return false;
        } 
 
